services:
  db:
    image: postgres:16
    ports:
      - "5432:5432"           # public bind

  runner:
    image: docker:cli
    privileged: true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /dev:/dev
    cap_add: ["ALL"]
    network_mode: host
