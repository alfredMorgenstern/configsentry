# Example: how a different repo would use ConfigSentry as an action.
# Copy into your own project and adjust the compose file path.

name: Compose lint (ConfigSentry)

on:
  push:
  pull_request:

jobs:
  configsentry:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Use the action from this repo (pin to a tag once we create releases)
      - uses: alfredMorgenstern/configsentry@master
        with:
          compose-file: docker-compose.yml
          sarif: true
